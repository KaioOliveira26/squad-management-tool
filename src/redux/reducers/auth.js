const defaultState = {
    auth: "",
    user: {},
    err: "",
  };
  
  const authentication = (state = defaultState, action) => {
    switch (action.type) {
  
      default:
        return state;
    }
  };
  
  export { authentication };
    